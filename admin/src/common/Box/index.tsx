import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const BoxColum = styled(Box)({
  display: 'flex',
  flexDirection: 'column',
});
export const BoxRow = styled(Box)({
  display: 'flex',
  flexDirection: 'row',
});
