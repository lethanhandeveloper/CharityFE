export interface BannerAPI {
  id: string;
  title: string;
  description: string;
  isActive: boolean;
}
export interface BannerUI {
  id: string;
  title: string;
  description: string;
  isActive: boolean;
  imageUrl: string;
}
